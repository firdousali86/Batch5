import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {backgroundColor: 'pink', flex: 1},
  button1: {height: 50, justifyContent: 'center', alignItems: 'center'},
});

export default styles;
